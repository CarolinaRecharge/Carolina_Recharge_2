# Carolina_Recharge_2
Reboot of Carolina Recharge

- **`index.html`** — the marketing site. Hand-coded, no build step. See
  `CLAUDE.md` for brand, voice and the compliance-critical language rules.
- **`demo/`** — the Site Energy Model demo, served at `carolinarecharge.com/demo/`.
  Static files, no build step, nothing to configure. Deliberately carries no
  product branding — see `CLAUDE.md` §10 and `demo/README.md` before changing that.
